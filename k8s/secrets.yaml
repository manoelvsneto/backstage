# Este é um exemplo de template. NÃO armazene segredos reais neste arquivo.
# Use kubectl create secret ou um gerenciador de segredos como Sealed Secrets ou Vault.
apiVersion: v1
kind: Secret
metadata:
  name: backstage-secrets
  namespace: backstage
type: Opaque
data:
  # Estes são valores de exemplo. Substitua com valores reais codificados em base64
  POSTGRES_USER: YmFja3N0YWdl     # backstage
  POSTGRES_PASSWORD: cGFzc3dvcmQ=  # password
  # Outros segredos, como chaves de API, tokens GitHub, etc.
